# Тестовые документы читаются и пишутся побайтово: round-trip сверяет
# байты, и нормализация переводов строк сделала бы сверку недостоверной.
*.siprj -text
*.silib -text

# Шелл-скрипты: CR в конце строки ломает bash на любой платформе.
*.sh text eol=lf
