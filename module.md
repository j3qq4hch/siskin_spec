# `<module>`

*определение модуля — переиспользуемый кусок схемы*

| | |
|---|---|
| **Встречается в** | проект |
| **Родители** | [`<project>`](project.md) |
| **Дети** | ровно два: [`<symbol>`](symbol.md) и [`<schematic>`](schematic.md) |

## Атрибуты

| Атрибут | Тип / единицы | Смысл |
|---|---|---|
| `name` | Строка | Имя модуля. Уникально в пределах проекта |
| `prefix` | Строка, [домен обозначения](component.md), **обязателен** | Префикс имён каналов — `DCDC` даёт `DCDC1`, `DCDC2` |

`prefix` устроен и работает **точно так же**, как [у компонента](component.md):
из него редактор строит имя [экземпляра](module-instance.md). Особого поведения у
модуля нет даже в мелочах — выводить префикс из имени модуля не надо.

**Умолчания у него, в отличие от компонента, нет.** Довода два, и оба про то, что
модуль — не библиотечная запись.

Единственный смысл модуля — **многоканальность**: ради одного вхождения его не
заводят. Значит каналов всегда несколько, префикс нужен всегда, и умолчание
пригодилось бы ровно ни разу.

А будь оно тем же `PART`, что у компонента, вышло бы хуже, чем бесполезно:
[имена деталей и каналов лежат в одном пространстве](pinref.md), и безымянные
каналы начали бы сталкиваться с безымянными деталями — в самом частом случае,
когда пользователь не задал ни там, ни там. Спросить префикс при создании ничего
не стоит: модуль [рождается явным жестом в схеме](#модуль-не-живёт-в-библиотеке),
а не приезжает из чужой библиотеки.

## Две половины: внешность и содержимое

```xml
<module name="DCDC" prefix="DCDC">
  <symbol>                                    <!-- ВНЕШНОСТЬ -->
    <shape x="0" y="0" w="20320" h="15240" outline="152" layer="94" />
    <pin name="VIN"    x="-10160" y="5080" rot="180000" direction="pwr" />
    <pin name="VDD_5V" x="10160"  y="0"    rot="0"      direction="pwr" />
    <text x="0" y="9000" height="1270" layer="95" align="center-center">&gt;NAME</text>
  </symbol>

  <schematic>                                 <!-- СОДЕРЖИМОЕ -->
    <part name="IC1" library="ti" component="TPS54360" device="-VQFN" />
    <compinst part="IC1" gate="" x="0" y="0" />
    <net name="FB"> … </net>
    <text x="0" y="-20000" height="1270" layer="99">про делитель</text>
    <variant name="3V3"> … </variant>
  </schematic>
</module>
```

| Половина | Что это |
|---|---|
| внешность | [`<symbol>`](symbol.md) — ровно как у компонента |
| содержимое | [`<schematic>`](schematic.md) — ровно как у проекта |

**Обе половины названы, и это не симметрия ради красоты.** Графика внутри модуля
бывает двух родов: контур тела и примечание к внутренней схеме — одинаковые
`<line>` и `<text>`. Без обёрток не отличить, что рисовать на верхнем уровне как
тело канала, а что показывать только внутри:

```xml
<symbol>
  <line … />                  контур тела — ВНЕШНОСТЬ канала
  <text>&gt;NAME</text>        подпись канала
</symbol>
<schematic>
  <line … />                  линия на внутренней схеме — ДЕКОРАЦИЯ
  <text>про делитель</text>   примечание, видно только внутри модуля
</schematic>
```

**Третьего места нет.** Раз у модуля ровно две половины и обе поименованы, объект,
лежащий прямо под `<module>`, не принадлежит ни одной — это ошибка, и её видно
сразу. Будь содержимое голыми детьми, любой мусор — опечатка, хвост кривого
импорта — молча становился бы частью внутренней схемы.

**Варианты сборки достаются даром.** Отдельного правила «у модуля тоже бывают
варианты» не нужно: [они есть у схемы](variant.md), а содержимое модуля и есть
схема.

**У этого символа нет имени.** Имя — это адрес в пуле, а символ модуля
используется **только этим модулем**: имени самого модуля уже достаточно. Тот же
принцип, что у [`<hole>`](hole.md) — имя есть у того, что адресуют.

**Внешность рисуется явно.** Прямоугольника «по умолчанию» нет. Размер, если он
понадобится целевому формату, выводится как габарит нарисованного, а не хранится.

### Схема модуля — та же самая схема

**Отличие от холста изделия ровно одно:** внутри не бывает
[`<modinst>`](module-instance.md), потому что вложенность запрещена — иерархия
ровно один уровень. Больше никаких изъятий нет, и это [та же
`<schematic>`](schematic.md) во всём остальном.

**Глобальные атрибуты у определения есть**, и отдельного правила под них не нужно
по той же причине, что и под варианты: содержимое модуля и есть схема, а
[у схемы атрибуты бывают](schematic.md#глобальные-атрибуты-у-неё-свои).

Роль у них ровно та же, что у [глобальных атрибутов холста
изделия](attr.md#глобальные-атрибуты--лист-исходных-данных) — **лист исходных
данных**, только свой: для содержимого [модуль и есть весь
документ](module-instance.md#изнутри-они-видны-как-атрибуты-документа), и
`>DOC@…` внутри читает именно его. Отсюда два употребления, оба знакомы по
верхнему уровню:

| Что здесь лежит | Зачем |
|---|---|
| **литералы** — параметры модуля и их умолчания | их читают формулы деталей внутри; [канал переопределяет](module-instance.md#объявляет-определение-значение-даёт-канал) |
| **результат вычисления, сделанного внутри** | показание: считается из своих же номиналов и рисуется [плейсхолдером на символе канала](module-instance.md#обратный-ход-показание-пишется-в-определении) |

И [совет](attr.md#но-это-совет-а-не-запрет) действует здесь слово в слово:
**слой ввода держат литералами.** Атрибут, чьё значение само начинается с `=`,
вычисляющие формулы ниже [прочитать не смогут](placeholders.md) — значит
параметром служит литерал, а формулой пишут **показание**, которое читают, а не
крутят. Ловит это линтер, а не запрет: отличие [по-прежнему ровно
одно](schematic.md#отличие-ровно-одно), и вторым его делать незачем.

**Слоёв нет ни у кого** — [таблица схемного пространства лежит у
проекта](layer-model.md), одна на холст изделия, все модули и все символы. Модуль
ничем не обделён: своей таблицы нет и у холста изделия.

**Рамки в ней есть**, и работают как везде: модуль печатают, как любую схему, и
[рамка](frame.md) задаёт, что попадёт на лист.

Печатается при этом **определение, а не канал**. Схема хранится один раз — своих
копий у каналов нет, они [ссылаются на неё по имени](module-instance.md).
Поэтому на листе стоят канонические имена деталей (`IC1`, а не `IC101`), а
плейсхолдеры разрешаются против **умолчаний определения** — незаполненные
[печатаются как есть](placeholders.md), как и везде. Это общий чертёж модуля в
его базовой настройке, а не одной его копии.

## Модуль не живёт в библиотеке

В этом его отличие от [компонента](component.md), и отсюда всё остальное. Модуль
рождается **в схеме**: сначала рисуют кусок, потом решают вывести часть сигналов
наружу.

Поэтому редактор схемы обязан уметь **режим правки модуля** — по сути тот же
редактор символа: рисуй тело, ставь выводы, расставляй плейсхолдеры.

**Вывод модуля именуется цепью внутри него.** Ставя пин, автор выбирает, какую
внутреннюю цепь вывести наружу — имя пина и есть имя этой цепи. Совпадение
буквальное: [капитализация имён выводов отменена](naming.md) в том числе поэтому
— пока она была, цепь `Vref` давала пин `VREF`, и равенство рвалось на ровном
месте.

> Разумно предлагать только цепи, которым человек **дал имя**: делать выводом цепь
> вида `N$4` бессмысленно. Но это правило **редактора**, а не формата — про `N$4`
> формат ничего не знает.

## Плейсхолдеры — как у символа

Определение несёт `>NAME` и ссылки на [атрибуты канала](module-instance.md);
каждый [экземпляр](module-instance.md) рисует их **своими** значениями, а его
список [`<text>`](text.md) переопределяет раскладку — ровно как у пары
«символ + [экземпляр компонента](component-instance.md)».

Для таблиц и выносок остаётся и второй путь — свободный текст со
[ссылкой на конкретный канал](placeholders.md): `>DCDC1@Vout`. Он не привязан к
модулю и годится там, где нужно свести параметры нескольких каналов разом.

## Секций у модуля не бывает

У [компонента](component.md) секции есть: физическая микросхема правда содержит
четыре независимых операционника. **У модуля их нет**, и это решение, а не
недоделка.

**Модуль с толпой выводов — признак плохой декомпозиции**, а не повод дать
инструмент для его разрезания. Подпирая такой модуль, мы бы поощряли то, что
следует переделать. Тот же довод, по которому отвергнут шинный порт модуля.

**И механизм не переносится.** Секции [компонента](component.md) работают потому,
что [`<gate>`](gate.md) ссылается на **пуловый** символ по имени. У модуля символ
вложенный и безымянный — адресовать одну секцию из нескольких было бы нечем.
Чтобы завести секции, пришлось бы сперва вернуть модульным символам имена и пул,
то есть отменить решение выше.

## Иерархия — ровно один уровень

**Модуль не вкладывается в модуль.** Многоканальная схема — единственный сценарий,
ради которого иерархия и заведена, и одного уровня ему достаточно.

Глубже начинается то, ради чего иерархию обычно заводят зря: группировка листов.
Она решается [страницами холста](schematic.md), а не вложенностью.

## Правила

- **`name` уникально в пределах проекта.**
- `prefix` задаётся явно и от `name` не зависит; домен у него [тот же, что у
  обозначения](component.md), — имена каналов и деталей [лежат в одном
  пространстве](pinref.md).
- Имя [пина](pin.md) модуля совпадает с именем внутренней цепи, которую он
  выводит — **буквально, до регистра**. Оба живут по [одному правилу](naming.md):
  написание сохраняется, сравнение идёт без учёта регистра. Написание берётся
  у цепи: она объявление, пин именуется ею.
- Внутри модуля нет [`<modinst>`](module-instance.md) — вложенность запрещена.
- Определение хранится один раз; сколько угодно каналов ссылаются на него по имени.
- **Имена деталей внутри канала** строятся префиксом `КАНАЛ:ДЕТАЛЬ` либо сдвигом
  нумерации, если у канала задан `offset` — см. [`<modinst>`](module-instance.md).
  Разрешённое имя обязано быть уникально в пределах схемы: не совпадать ни с
  именами компонентов верхнего уровня, ни с разрешёнными именами других каналов.
- **Имена внутренних цепей** строятся только префиксом `КАНАЛ:ЦЕПЬ`: `offset`
  двигает числа, а имя цепи цифр может не иметь вовсе. Цепь, выведенная
  [пином](pin.md), имени не меняет — [она сливается с родительской](net.md).
- **[`<symbol>`](symbol.md) ровно один.** Ноль — у канала нет ни тела, ни
  выводов, его нечем нарисовать и не к чему подключиться. Секций у модуля
  [не бывает](#секций-у-модуля-не-бывает), поэтому второго символа не может
  понадобиться.
- **Тело модуля не рисуют на слое `98`.** [`<shape>` на нём делает символ
  рамкой](frame.md), а рамка выводов не несёт — подключиться к такому каналу
  нечем. Отказ случается при правке модуля, там же, где символ и рисуют.
- **[`<schematic>`](schematic.md) ровно один**, и других детей у `<module>` нет:
  объект под самим `<module>` не принадлежит ни внешности, ни содержимому.

---

[<- Оглавление](README.md)
