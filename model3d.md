# `<model3d>`

*привязка 3D-модели к корпусу*

| | |
|---|---|
| **Встречается в** | корпус |
| **Родители** | [`<footprint>`](footprint.md) |
| **Дети** | — |

## Атрибуты

| Атрибут | Тип / единицы | Смысл |
|---|---|---|
| `key` | Строка | Ключ выбора: **голое значение** атрибута [детали](#экземпляр--это-деталь) (`RED`, `H11`) — не имя атрибута. Сравнивается [без учёта регистра](#ключ-сравнивается-без-учёта-регистра). Запись без `key` — безусловный fallback, максимум одна |
| `tx` `ty` `tz` | Целое, [µm](units.md), умолчание `0` | Смещение [в системе координат корпуса](#размещение-полная-матрица). У каждой записи своё |
| `rx` `ry` `rz` | Целое, [мград](units.md), умолчание `0` | Поворот, **MCAD intrinsic XYZ** |

## Имя файла модели в IR не хранится

Оно **выводится из имени [`<footprint>`](footprint.md)**:

| Запись | Основа имени |
|---|---|
| без `key` | `LED0603` |
| `key="RED"` | `LED0603_RED` |

Отсюда практическое следствие для именования корпусов: называть их
`R0603`, а не `0603` — иначе модели разных семейств столкнутся именами.

### Библиотека в имя файла не входит

Корпуса в проекте [различаются парой `(library, name)`](footprint.md), а модель
выбирается **одним именем**. Это не упущение, а решение.

`R0603` — **физический объект**, один и тот же, из какой бы библиотеки он ни
пришёл. Две библиотеки, описавшие этот корпус, описали одну и ту же вещь; держать
под неё две одинаковых модели незачем, а спрашивать «модель какой библиотеки»
попросту не о чем.

Поэтому папка моделей — **не часть пула**, а склад физических форм, общий на
проект. Ключ у него имя корпуса, и совпадение имён там не коллизия, а попадание.

Цена та же, что названа выше и другого механизма не имеет: назвал корпус `0603`
вместо `R0603` — получишь чужую модель. Дисциплина имени здесь и есть защита.

Отсюда же [запрет двоеточия в имени корпуса](footprint.md): имя едет в файловую
систему как есть, а двоеточие в имени файла на Windows незаконно.

## Файлов два: STEP и GLB

Основа имени одна, расширения два — и оба обязаны лежать рядом:

| Файл | Что это | Кому нужен |
|---|---|---|
| `LED0603.step` | **источник** — то, что принёс пользователь | выгрузке STEP-модели платы |
| `LED0603.glb` | **рабочая** — тесселированная при импорте | отрисовке |

**Ввод — только STEP.** Формат один, потому что он единственный несёт **тело**, а
не поверхность: из него получается и картинка, и корректная сборка платы для
механиков. Принимать вдобавок сеточные форматы значило бы завести модели, которые
выглядят, но в STEP платы не собираются, — и разбираться потом, почему одна
деталь выгрузилась, а соседняя нет.

**Хранится и рисуется GLB.** Тесселяция делается **один раз, на входе** — там же,
[где происходит всякая потеря точности](units.md). Разбирать STEP при каждом
открытии платы с сотней корпусов немыслимо: это тяжёлый параметрический формат, а
на экране от него нужен треугольник.

**STEP при этом не выбрасывается.** Плату выгружают в STEP целиком — механику, в
корпус, на согласование, — и собирать её из тесселированных сеток нельзя: тела
исчезли, остались оболочки. Значит источник обязан пережить импорт.

| Чего нет | Что теряется |
|---|---|
| `.step` | плата выгружается в STEP без этой детали, с записью в лог |
| `.glb` | ничего: редактор тесселирует источник при первом открытии |

Асимметрия неслучайна: `.glb` **производен** и восстановим, `.step` — исходные
данные, и восстановить его не из чего.

**Единицы и ориентация — забота файла, а не IR.** STEP несёт свои единицы в себе,
у glTF они [заданы спецификацией](https://registry.khronos.org/glTF/specs/2.0/glTF-2.0.html);
перевод делает тесселятор. В IR от модели остаётся только размещение — `tx`/`ty`/`tz`
в [µm](units.md) и `rx`/`ry`/`rz` в мград.

> ⚠ Модель висит на **корпусе**, а не на [устройстве](device.md). Устройства,
> делящие один корпус, делят и модель — иначе пришлось бы держать две одинаковых.

## Где лежат файлы моделей

Рядом с файлом проекта, в папке `%имя_проекта%_models` — тот же шаблон
`%имя%_<что>`, что у [картинок заметок](note.md):

```
Base.siprj
Base_models/          ← сюда
Base_figures/
```

Обе версии каждой модели — там же и под одним именем: `LED0603.step` рядом с
`LED0603.glb`.

**Внутрь проекта модели не встраиваются.** Бинарное в текстовом — дурной тон:
`.siprj` перестал бы читаться глазами и diff'ом, а выигрыш был бы только в числе
файлов. Самодостаточность держится папкой рядом, а не встраиванием.

**Глобальное хранилище моделей — дело редактора.** Он ищет модель сначала в
папке проекта, потом у себя: [ближний побеждает](attr.md) буквально, тем же
правилом, что у атрибутов. Поэтому «передать проект целиком» — это копирование
найденного в `%имя_проекта%_models`, а не переписывание файла проекта; после
копирования редактор берёт модели уже оттуда.

### Папка — спутник файла, а не проекта

Имя папки берётся от **имени файла**, а не от [`project@name`](project.md):
`Base.siprj` даёт `Base_models/`, как бы ни называлось изделие внутри. Имя
изделия живёт в документе, папка — в файловой системе, и связывать их значило бы
искать папку по содержимому файла.

Отсюда её жизнь целиком в руках редактора:

| Операция над файлом | Что с папкой |
|---|---|
| переименование | переносится вместе с ним |
| «сохранить как» | **копируется** рядом с новым файлом |

Копия — не расточительство: в папке проекта лежит не склад форм целиком, а
только то, что передают вместе с проектом, и основная масса моделей остаётся у
редактора. Переименовали файл мимо редактора — старую папку никто не ищет:
модели найдутся в глобальном хранилище, и это та же мягкая деградация, что при
потерянном файле.

**У standalone-библиотеки шаблон тот же:** `rc.silib` даёт `rc_models/`. Корпус
с `<model3d>` [законен и вне проекта](library.md), а взяться папке рядом больше
неоткуда.

### Имя корпуса — имя файла, а не путь

Неудобные для файловой системы символы в именах [не
запрещены](naming.md) — но путём от этого не становятся. **Разрешённый путь
обязан лежать внутри папки моделей**: вышел за неё — [заглушка и
диагностика](#диспетчеризация-одно-посадочное-параметричная-внешность), а не
чтение и не запись.

Проверка стоит копейки, а без неё имя вроде `../../ключ` не «не загрузится, и
это видно», а тихо читает чужой файл — и, поскольку `.glb` [тесселируется на
входе](#файлов-два-step-и-glb), тихо пишет рядом с ним. С проверкой [довод
naming.md](naming.md) снова верен: напоретесь — не загрузится.

## Диспетчеризация: одно посадочное, параметричная внешность

У корпуса может быть **несколько** `<model3d>`. Класс задач — одно посадочное
место при разной внешности: цвет светодиода, высота гребёнки, актуатор кнопки.

**Резолюция по экземпляру:** ключи ищутся среди значений **всех** его
[атрибутов](attr.md).

| Совпадений | Результат |
|---|---|
| 0 | Fallback-запись (та, что без `key`) |
| 1 | Она и берётся |
| ≥2 разных ключа | **Конфликт**: заглушка и диагностика. Никогда не «первый попавшийся» |

Композитная внешность = композитное значение-ключ: `RED-GREEN` у двухцветного
светодиода.

### Экземпляр — это деталь

У [`<element>`](element.md) атрибутов нет вовсе: он несёт имя, координаты,
поворот и сторону. Искать ключ поэтому негде, кроме как у [детали](part.md), и
берётся её **эффективный набор** — тот же, что читают плейсхолдеры:
[вариант > деталь > устройство > компонент](attr.md).

Глобальные атрибуты документа в поиск не входят: [`>DOC@…`](placeholders.md) —
свойство листа, а не этой детали, и цвет светодиода из него не следует.

**Вариант сборки участвует, и это главное.** Он ближайшее звено цепочки: перекрыл
`COLOR` — сменилась модель. В окне 3D всегда стоит модель, актуальная для
**выбранной сейчас** сборки, а сам выбор [в файле не хранится](variant.md) —
картинка производна от него, как и BOM.

**Деталь, погашенная вариантом, в модель не попадает вовсе.**
[`exclude="1"`](variant.md) значит «не устанавливается», а 3D показывает
**собранную** плату: ни в окне просмотра, ни в STEP-выгрузке такой детали нет.
Посадочное место при этом остаётся на месте — [медь и отверстия отштампованы до
всякой сборки](variant.md), и вариантом их не отменить.

### Ключ сравнивается без учёта регистра

`key="RED"` находит и `Red`, и `red`. Правило идёт вразрез с общим [«в значении
регистр значим»](naming.md), и довод у него свой: **ключ уезжает в имя файла**.

`LED0603_RED.step` и `LED0603_red.step` на Windows — **один и тот же файл**.
Значит различать `RED` и `red` формат не может при всём желании: на одной
платформе это два ключа, на другой один. Выбирать приходится из двух зол, и
меньшее — считать их одним всюду.

- **Ключи корпуса уникальны без учёта регистра**: `RED` и `red` — не два ключа, а
  один, объявленный дважды.
- **Файл ищется так же.** Проект, собранный на Windows с `LED0603_red.step`,
  обязан открыться на Linux с `key="RED"` — иначе платформа автора становится
  частью формата.
- **Неоднозначность — отказ.** На Linux в папке законно лежат сразу
  `LED0603_RED.step` и `LED0603_red.step`. Который из них «тот» — вопрос без
  ответа, и формат его не угадывает: заглушка и диагностика, [как при конфликте
  ключей](#диспетчеризация-одно-посадочное-параметричная-внешность). А что два
  таких файла лежат рядом, говорит линтер.

**У имени корпуса развилка та же, и решается так же.** `R0603` и `r0603` — [два
законных имени](naming.md), но один файл на Windows и два на Linux. Поиск файла
регистра не различает нигде, а два подходящих файла дают отказ, а не выбор:
платформа автора не становится частью формата ни с той половины имени, ни с
этой.

Написание при этом сохраняется как введено, [как у имени атрибута](naming.md): в
имя файла едет ровно то, что написал автор.

## Размещение: полная матрица

Запись `<model3d>` лежит в **корпусе**, а на плату корпус ставит
[`<element>`](element.md). Мировое положение поэтому складывается из двух
половин, и складывается однозначно:

```
world = T(ex, ey, z_side) · Rz(rot) · F_side · T(tx, ty, tz) · R(rx, ry, rz)

F_top = I                 F_bottom = Ry(180°)
R     = Rx(rx) · Ry(ry) · Rz(rz)        MCAD intrinsic XYZ, Z-up, как у Altium
```

Читается справа налево: правая половина своя у модели, левая — [та же самая, что
у плоского корпуса](element.md), сторона-поворот-перенос.

**`tx ty tz` заданы в системе координат корпуса** — той, [в которой нарисованы
пады](element.md). Иначе и быть не может: `R0603` стоит на плате десятки раз, а
запись `<model3d>` у него одна.

**Сначала поворот вокруг начала модели, затем трансляция** — матрично `T · R`.
Перепутать с `R · T` нельзя: сдвиг «поедет» именно у повёрнутых корпусов, и баг
выйдет коварный — у неповёрнутых всё выглядит правильно. Следствие: оси сдвига
**от `rx ry rz` не зависят**. Это MCAD-стандарт.

### Снизу — переворот, а не отражение

`F_bottom = Ry(180°)` — **поворот**, определитель `+1`. Хиральность цела, нормали
не выворачиваются, левых резьб и зеркальных винтов не заводится: деталь снизу —
та же деталь, а не её отражение. Отражать тело было бы физически неверно, и в
STEP-выгрузку такая сборка уехала бы браком.

Второй конвенции при этом не появляется. Тень `Ry(180°)` на плоскости платы — это
`X → −X`, ровно [то зеркало, что задано плоскому корпусу](element.md). Плоскость
и объём говорят одно и то же разными словами.

**`tz` — высота над своей стороной.** Знак автору менять не нужно: `Ry(180°)`
уносит модель вниз сам, и `tz="500"` значит «на 500 µm наружу от поверхности» на
обеих сторонах.

### `z = 0` — поверхность платы

Ноль лежит **на верхней поверхности стека**, и от него считается всё:

| Сторона | `z_side` |
|---|---|
| `top` | `0` |
| `bottom` | `−` сумма толщин [формулы стека](layout.md) |

Толщину рендер складывает из стека сам — арифметика тривиальная. Альтернатива,
ноль в средней плоскости, симметричнее, но стоила бы того, что `tz` перестал бы
быть высотой над платой.

## Правила

- Запись без `key` — **максимум одна** на корпус.
- `key` — значение атрибута, не имя. `RED`, а не `COLOR`.
- **Ключ ищется у [детали](part.md)**, в её эффективном наборе
  [вариант > деталь > устройство > компонент](attr.md); у
  [элемента](element.md) атрибутов нет, глобальные атрибуты документа не
  участвуют. У детали **внутри канала** первым звеном стоит [вариант, выбранный
  каналом](module-instance.md), — он, в отличие от варианта верхнего уровня,
  хранится в файле.
- **Сравнение без учёта регистра**, и ключи корпуса уникальны так же: `RED` и
  `red` — [один ключ](#ключ-сравнивается-без-учёта-регистра), потому что имя
  файла на Windows регистра не различает. По тому же правилу ищется и файл, а
  **два файла, различающиеся регистром, дают отказ, а не выбор** — правило
  накрывает обе половины имени, и ключ, и имя корпуса.
- **Погашенная [вариантом](variant.md) деталь в модель не попадает** — ни в окно
  просмотра, ни в STEP-выгрузку; посадочное место на плате при этом остаётся.
- Конфликт ключей разрешается **заглушкой**, а не выбором наугад.
- **Папка моделей названа по имени файла**, не по [`project@name`](project.md),
  и ходит за файлом: переименование её переносит, «сохранить как» — копирует. У
  [standalone-библиотеки](library.md) шаблон тот же.
- **Путь обязан разрешиться внутри папки моделей.** Имя корпуса и ключ едут в
  имя файла как есть, но путём не становятся: вышло наружу — заглушка, а не
  чтение и не запись.
- **Мировая матрица** — `T(ex, ey, z_side) · Rz(rot) · F_side · T(tx, ty, tz) ·
  R(rx, ry, rz)`. `tx ty tz` заданы в системе координат корпуса, `z = 0` —
  верхняя поверхность стека, `F_bottom = Ry(180°)`: снизу деталь **переворачивают,
  а не отражают**, и `tz` остаётся высотой над своей стороной.
- **Ввод только STEP.** Он один несёт тело; сеточные форматы дали бы модели,
  которые видно, но в STEP платы не собрать.
- **Файлов два на модель** — `.step` и `.glb`, одна основа имени. Рисуется GLB,
  выгружается STEP; тесселяция делается один раз, на входе.
- Пропал `.glb` — тесселируется заново. Пропал `.step` — деталь **выпадает из
  STEP-выгрузки платы**, с записью в лог.

---

[<- Оглавление](README.md)
